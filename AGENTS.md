# Cough App - AI Agent ガイドライン (AGENTS.md)

このドキュメントは、このリポジトリ（`cough-app`）を操作、機能追加、またはデバッグする AI コーディングエージェント（Antigravity など）に向けた開発者ガイドおよびコンテキスト共有ファイルです。

---

## 1. プロジェクト概要

`cough-app` は、咳（Cough）の音声を再生および録音・アップロードし、管理・分析するための Next.js アプリケーションです。

- **主要スタック**:
  - **フレームワーク**: Next.js 16 (App Router, Node.js ランタイム)
  - **ランタイム**: React 19 / TypeScript / Bun (または npm)
  - **スタイリング**: Tailwind CSS v4 (PostCSS)
  - **UI コンポーネント**: shadcn (Radix UI)
  - **音声処理**: `react-media-recorder` によるマイク録音
  - **ストレージ**: `@vercel/blob` によるクラウド音声ファイル保存

---

## 2. ディレクトリ構成

主なフォルダおよびファイルとその役割は以下の通りです。

```text
cough-app/
├── app/
│   ├── api/
│   │   └── route.ts         # Vercel Blob への音声ファイルアップロード API
│   ├── favicon.ico
│   ├── globals.css          # Tailwind CSS のグローバル定義
│   ├── layout.tsx           # ルートレイアウト
│   └── page.tsx             # メイン画面（再生・録音 UI の統合）
├── components/
│   ├── PlayButton.tsx       # 咳音声サンプルの再生ボタン
│   ├── RecordButton.tsx     # マイク録音およびアップロード機能 (Dialog)
│   └── ui/                  # shadcn UI コンポーネント群 (Radix UI)
│       ├── button.tsx
│       ├── card.tsx
│       ├── dialog.tsx
│       ├── popover.tsx
│       └── tooltip.tsx
├── coughs/                  # ローカル開発時の仮置きまたはローカル参照領域
├── package.json             # 依存関係およびスクリプト定義
└── tsconfig.json            # TypeScript 設定
```

---

## 3. 主要機能とデータフロー

### A. サンプル音声の再生 (`PlayButton.tsx`)

- インデックス（`0` 〜 `5`）に対応するサンプル音声を再生するプレースホルダーボタン。
- 現状はクリック時の音声再生ロジックが未実装、またはカスタマイズ対象です。

### B. 音声の録音とプレビュー (`RecordButton.tsx`)

- `react-media-recorder` を使用してブラウザでマイク入力を受け取り、WebM などの形式で録音します。
- 録音完了後、`Dialog` 内の `<audio>` 要素でプレビュー再生が可能です。
- 再録音（Re-record）とアップロード（Upload）の切り替えが行えます。

### C. クラウド保存 API (`app/api/route.ts`)

- フロントエンドから送られてきた `FormData` (`audio` フィールド) を受け取ります。
- 拡張子は MIME タイプまたはファイル名から自動判別されます。
- `@vercel/blob` の `put` メソッドを呼び出し、`coughs/${filename}` の形式でパブリック保存します。

---

## 4. エージェント開発・編集ルール

AI エージェントが本リポジトリを変更する際は、以下の設計・実装ルールを厳守してください。

### ① スタイリング & UI コントロール

- **Tailwind CSS v4** を使用しています。カスタムユーティリティを直接インラインで書きすぎるのではなく、必要に応じて `@theme` や `globals.css` と連携してください。
- shadcn UI コンポーネント (`components/ui`) を優先して使用し、デザインの統一性を保ってください。
- インタラクティブな操作には、必ず適切なホバー効果、ローディング表示（スピナーなど）、および確認ダイアログを取り入れ、プレミアムでリッチなユーザー体験を提供してください。

### ② コンポーネント境界

- クライアントサイドでの状態管理やブラウザ API（マイク録音など）が必要なコンポーネントには、必ず先頭に `"use client"` を付与してください。
- サーバーコンポーネントとクライアントコンポーネントの責務を分離し、不要なハイドレーションエラーを防いでください。

### ③ インポートパス

- エイリアスパス `@/*` を使用してインポートを行います（例: `@/components/ui/button`）。相対パス（`../../components` など）は極力避け、スマートに記述してください。

### ④ エラーハンドリング & ロギング

- API 呼び出しやファイルアップロード処理の周辺には、必ず適切な `try-catch` ブロックを設け、失敗時にはユーザーにフレンドリーなエラーメッセージを表示してください。
- サーバー側ログは `console.error(error)` などで原因追跡ができるように出力してください。

---

## 5. コマンドライン操作

ローカル開発環境での動作確認や実行には、以下のコマンドを使用します。

- **開発サーバー起動**:

  ```bash
  bun run dev
  ```

  ※デフォルトポートは `43875` にバインドされています (`next dev -p 43875`)。

- **プロジェクトのビルド**:

  ```bash
  bun run build
  ```

- **本番サーバーの起動**:

  ```bash
  bun run start
  ```

- **静的解析・検証**:
  ```bash
  bun run lint
  ```
